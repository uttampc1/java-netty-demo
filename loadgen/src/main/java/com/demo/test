echo
echo "Bare repository created successfully:"
echo "  $REPO_PATH"
echo
echo "Default branch set to: ${DEFAULT_BRANCH}"
echo
echo "To add this as a remote to an existing local project:"
echo
echo "  cd /path/to/your/project"
echo "  git remote add origin git@<server>:${REPO_NAME}.git"
echo "  git branch -M ${DEFAULT_BRANCH}"
echo "  git push -u origin ${DEFAULT_BRANCH}"
echo
echo "To create a new project with README.md and push it:"
echo
echo "  mkdir ${REPO_NAME} && cd ${REPO_NAME}"
echo "  git init"
echo "  git branch -M ${DEFAULT_BRANCH}"
echo "  echo \"# ${REPO_NAME}\" > README.md"
echo "  git add README.md"
echo "  git commit -m \"Initial commit\""
echo "  git remote add origin git@<server>:${REPO_NAME}.git"
echo "  git push -u origin ${DEFAULT_BRANCH}"
echo
